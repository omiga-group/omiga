// Code generated by ent, DO NOT EDIT.

package repositories

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/omiga-group/omiga/src/exchange/shared/repositories/exchange"
	"github.com/omiga-group/omiga/src/exchange/shared/repositories/internal"
	"github.com/omiga-group/omiga/src/exchange/shared/repositories/predicate"
	"github.com/omiga-group/omiga/src/exchange/shared/repositories/tradingpairs"
)

// TradingPairsQuery is the builder for querying TradingPairs entities.
type TradingPairsQuery struct {
	config
	limit        *int
	offset       *int
	unique       *bool
	order        []OrderFunc
	fields       []string
	predicates   []predicate.TradingPairs
	withExchange *ExchangeQuery
	withFKs      bool
	loadTotal    []func(context.Context, []*TradingPairs) error
	modifiers    []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the TradingPairsQuery builder.
func (tpq *TradingPairsQuery) Where(ps ...predicate.TradingPairs) *TradingPairsQuery {
	tpq.predicates = append(tpq.predicates, ps...)
	return tpq
}

// Limit adds a limit step to the query.
func (tpq *TradingPairsQuery) Limit(limit int) *TradingPairsQuery {
	tpq.limit = &limit
	return tpq
}

// Offset adds an offset step to the query.
func (tpq *TradingPairsQuery) Offset(offset int) *TradingPairsQuery {
	tpq.offset = &offset
	return tpq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (tpq *TradingPairsQuery) Unique(unique bool) *TradingPairsQuery {
	tpq.unique = &unique
	return tpq
}

// Order adds an order step to the query.
func (tpq *TradingPairsQuery) Order(o ...OrderFunc) *TradingPairsQuery {
	tpq.order = append(tpq.order, o...)
	return tpq
}

// QueryExchange chains the current query on the "exchange" edge.
func (tpq *TradingPairsQuery) QueryExchange() *ExchangeQuery {
	query := &ExchangeQuery{config: tpq.config}
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := tpq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := tpq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(tradingpairs.Table, tradingpairs.FieldID, selector),
			sqlgraph.To(exchange.Table, exchange.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, tradingpairs.ExchangeTable, tradingpairs.ExchangeColumn),
		)
		schemaConfig := tpq.schemaConfig
		step.To.Schema = schemaConfig.Exchange
		step.Edge.Schema = schemaConfig.TradingPairs
		fromU = sqlgraph.SetNeighbors(tpq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first TradingPairs entity from the query.
// Returns a *NotFoundError when no TradingPairs was found.
func (tpq *TradingPairsQuery) First(ctx context.Context) (*TradingPairs, error) {
	nodes, err := tpq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{tradingpairs.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (tpq *TradingPairsQuery) FirstX(ctx context.Context) *TradingPairs {
	node, err := tpq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first TradingPairs ID from the query.
// Returns a *NotFoundError when no TradingPairs ID was found.
func (tpq *TradingPairsQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = tpq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{tradingpairs.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (tpq *TradingPairsQuery) FirstIDX(ctx context.Context) int {
	id, err := tpq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single TradingPairs entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one TradingPairs entity is found.
// Returns a *NotFoundError when no TradingPairs entities are found.
func (tpq *TradingPairsQuery) Only(ctx context.Context) (*TradingPairs, error) {
	nodes, err := tpq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{tradingpairs.Label}
	default:
		return nil, &NotSingularError{tradingpairs.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (tpq *TradingPairsQuery) OnlyX(ctx context.Context) *TradingPairs {
	node, err := tpq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only TradingPairs ID in the query.
// Returns a *NotSingularError when more than one TradingPairs ID is found.
// Returns a *NotFoundError when no entities are found.
func (tpq *TradingPairsQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = tpq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{tradingpairs.Label}
	default:
		err = &NotSingularError{tradingpairs.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (tpq *TradingPairsQuery) OnlyIDX(ctx context.Context) int {
	id, err := tpq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of TradingPairsSlice.
func (tpq *TradingPairsQuery) All(ctx context.Context) ([]*TradingPairs, error) {
	if err := tpq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return tpq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (tpq *TradingPairsQuery) AllX(ctx context.Context) []*TradingPairs {
	nodes, err := tpq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of TradingPairs IDs.
func (tpq *TradingPairsQuery) IDs(ctx context.Context) ([]int, error) {
	var ids []int
	if err := tpq.Select(tradingpairs.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (tpq *TradingPairsQuery) IDsX(ctx context.Context) []int {
	ids, err := tpq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (tpq *TradingPairsQuery) Count(ctx context.Context) (int, error) {
	if err := tpq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return tpq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (tpq *TradingPairsQuery) CountX(ctx context.Context) int {
	count, err := tpq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (tpq *TradingPairsQuery) Exist(ctx context.Context) (bool, error) {
	if err := tpq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return tpq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (tpq *TradingPairsQuery) ExistX(ctx context.Context) bool {
	exist, err := tpq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the TradingPairsQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (tpq *TradingPairsQuery) Clone() *TradingPairsQuery {
	if tpq == nil {
		return nil
	}
	return &TradingPairsQuery{
		config:       tpq.config,
		limit:        tpq.limit,
		offset:       tpq.offset,
		order:        append([]OrderFunc{}, tpq.order...),
		predicates:   append([]predicate.TradingPairs{}, tpq.predicates...),
		withExchange: tpq.withExchange.Clone(),
		// clone intermediate query.
		sql:    tpq.sql.Clone(),
		path:   tpq.path,
		unique: tpq.unique,
	}
}

// WithExchange tells the query-builder to eager-load the nodes that are connected to
// the "exchange" edge. The optional arguments are used to configure the query builder of the edge.
func (tpq *TradingPairsQuery) WithExchange(opts ...func(*ExchangeQuery)) *TradingPairsQuery {
	query := &ExchangeQuery{config: tpq.config}
	for _, opt := range opts {
		opt(query)
	}
	tpq.withExchange = query
	return tpq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Symbol string `json:"symbol,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.TradingPairs.Query().
//		GroupBy(tradingpairs.FieldSymbol).
//		Aggregate(repositories.Count()).
//		Scan(ctx, &v)
func (tpq *TradingPairsQuery) GroupBy(field string, fields ...string) *TradingPairsGroupBy {
	grbuild := &TradingPairsGroupBy{config: tpq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := tpq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return tpq.sqlQuery(ctx), nil
	}
	grbuild.label = tradingpairs.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Symbol string `json:"symbol,omitempty"`
//	}
//
//	client.TradingPairs.Query().
//		Select(tradingpairs.FieldSymbol).
//		Scan(ctx, &v)
func (tpq *TradingPairsQuery) Select(fields ...string) *TradingPairsSelect {
	tpq.fields = append(tpq.fields, fields...)
	selbuild := &TradingPairsSelect{TradingPairsQuery: tpq}
	selbuild.label = tradingpairs.Label
	selbuild.flds, selbuild.scan = &tpq.fields, selbuild.Scan
	return selbuild
}

func (tpq *TradingPairsQuery) prepareQuery(ctx context.Context) error {
	for _, f := range tpq.fields {
		if !tradingpairs.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("repositories: invalid field %q for query", f)}
		}
	}
	if tpq.path != nil {
		prev, err := tpq.path(ctx)
		if err != nil {
			return err
		}
		tpq.sql = prev
	}
	return nil
}

func (tpq *TradingPairsQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*TradingPairs, error) {
	var (
		nodes       = []*TradingPairs{}
		withFKs     = tpq.withFKs
		_spec       = tpq.querySpec()
		loadedTypes = [1]bool{
			tpq.withExchange != nil,
		}
	)
	if tpq.withExchange != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, tradingpairs.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*TradingPairs).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &TradingPairs{config: tpq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	_spec.Node.Schema = tpq.schemaConfig.TradingPairs
	ctx = internal.NewSchemaConfigContext(ctx, tpq.schemaConfig)
	if len(tpq.modifiers) > 0 {
		_spec.Modifiers = tpq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, tpq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := tpq.withExchange; query != nil {
		if err := tpq.loadExchange(ctx, query, nodes, nil,
			func(n *TradingPairs, e *Exchange) { n.Edges.Exchange = e }); err != nil {
			return nil, err
		}
	}
	for i := range tpq.loadTotal {
		if err := tpq.loadTotal[i](ctx, nodes); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (tpq *TradingPairsQuery) loadExchange(ctx context.Context, query *ExchangeQuery, nodes []*TradingPairs, init func(*TradingPairs), assign func(*TradingPairs, *Exchange)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*TradingPairs)
	for i := range nodes {
		if nodes[i].exchange_trading_pairs == nil {
			continue
		}
		fk := *nodes[i].exchange_trading_pairs
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	query.Where(exchange.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "exchange_trading_pairs" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (tpq *TradingPairsQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := tpq.querySpec()
	_spec.Node.Schema = tpq.schemaConfig.TradingPairs
	ctx = internal.NewSchemaConfigContext(ctx, tpq.schemaConfig)
	if len(tpq.modifiers) > 0 {
		_spec.Modifiers = tpq.modifiers
	}
	_spec.Node.Columns = tpq.fields
	if len(tpq.fields) > 0 {
		_spec.Unique = tpq.unique != nil && *tpq.unique
	}
	return sqlgraph.CountNodes(ctx, tpq.driver, _spec)
}

func (tpq *TradingPairsQuery) sqlExist(ctx context.Context) (bool, error) {
	n, err := tpq.sqlCount(ctx)
	if err != nil {
		return false, fmt.Errorf("repositories: check existence: %w", err)
	}
	return n > 0, nil
}

func (tpq *TradingPairsQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   tradingpairs.Table,
			Columns: tradingpairs.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: tradingpairs.FieldID,
			},
		},
		From:   tpq.sql,
		Unique: true,
	}
	if unique := tpq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := tpq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, tradingpairs.FieldID)
		for i := range fields {
			if fields[i] != tradingpairs.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := tpq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := tpq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := tpq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := tpq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (tpq *TradingPairsQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(tpq.driver.Dialect())
	t1 := builder.Table(tradingpairs.Table)
	columns := tpq.fields
	if len(columns) == 0 {
		columns = tradingpairs.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if tpq.sql != nil {
		selector = tpq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if tpq.unique != nil && *tpq.unique {
		selector.Distinct()
	}
	t1.Schema(tpq.schemaConfig.TradingPairs)
	ctx = internal.NewSchemaConfigContext(ctx, tpq.schemaConfig)
	selector.WithContext(ctx)
	for _, m := range tpq.modifiers {
		m(selector)
	}
	for _, p := range tpq.predicates {
		p(selector)
	}
	for _, p := range tpq.order {
		p(selector)
	}
	if offset := tpq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := tpq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (tpq *TradingPairsQuery) ForUpdate(opts ...sql.LockOption) *TradingPairsQuery {
	if tpq.driver.Dialect() == dialect.Postgres {
		tpq.Unique(false)
	}
	tpq.modifiers = append(tpq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return tpq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (tpq *TradingPairsQuery) ForShare(opts ...sql.LockOption) *TradingPairsQuery {
	if tpq.driver.Dialect() == dialect.Postgres {
		tpq.Unique(false)
	}
	tpq.modifiers = append(tpq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return tpq
}

// Modify adds a query modifier for attaching custom logic to queries.
func (tpq *TradingPairsQuery) Modify(modifiers ...func(s *sql.Selector)) *TradingPairsSelect {
	tpq.modifiers = append(tpq.modifiers, modifiers...)
	return tpq.Select()
}

// TradingPairsGroupBy is the group-by builder for TradingPairs entities.
type TradingPairsGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (tpgb *TradingPairsGroupBy) Aggregate(fns ...AggregateFunc) *TradingPairsGroupBy {
	tpgb.fns = append(tpgb.fns, fns...)
	return tpgb
}

// Scan applies the group-by query and scans the result into the given value.
func (tpgb *TradingPairsGroupBy) Scan(ctx context.Context, v any) error {
	query, err := tpgb.path(ctx)
	if err != nil {
		return err
	}
	tpgb.sql = query
	return tpgb.sqlScan(ctx, v)
}

func (tpgb *TradingPairsGroupBy) sqlScan(ctx context.Context, v any) error {
	for _, f := range tpgb.fields {
		if !tradingpairs.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := tpgb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := tpgb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (tpgb *TradingPairsGroupBy) sqlQuery() *sql.Selector {
	selector := tpgb.sql.Select()
	aggregation := make([]string, 0, len(tpgb.fns))
	for _, fn := range tpgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	// If no columns were selected in a custom aggregation function, the default
	// selection is the fields used for "group-by", and the aggregation functions.
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(tpgb.fields)+len(tpgb.fns))
		for _, f := range tpgb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(tpgb.fields...)...)
}

// TradingPairsSelect is the builder for selecting fields of TradingPairs entities.
type TradingPairsSelect struct {
	*TradingPairsQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Scan applies the selector query and scans the result into the given value.
func (tps *TradingPairsSelect) Scan(ctx context.Context, v any) error {
	if err := tps.prepareQuery(ctx); err != nil {
		return err
	}
	tps.sql = tps.TradingPairsQuery.sqlQuery(ctx)
	return tps.sqlScan(ctx, v)
}

func (tps *TradingPairsSelect) sqlScan(ctx context.Context, v any) error {
	rows := &sql.Rows{}
	query, args := tps.sql.Query()
	if err := tps.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (tps *TradingPairsSelect) Modify(modifiers ...func(s *sql.Selector)) *TradingPairsSelect {
	tps.modifiers = append(tps.modifiers, modifiers...)
	return tps
}
